Continued the full react course.

- learnt about the useReducer
- useReducer(reducer, initialState)
- reducer(state,action)=>{
  return newState
  }
- useContext to transfer property values from parent to children and gradchildren
